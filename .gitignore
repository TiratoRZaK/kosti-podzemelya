# Исходники генерации ассетов (архивы, промежуточные PNG) держим локально:
# в репозиторий попадает только то, что реально используется игрой.
sources/

# Собранные APK — артефакт сборки, не исходник (28 МБ на каждую сборку).
build/
godot/.godot/
godot/android/
